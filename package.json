{
  "name": "@fateforge/creo-cli",
  "version": "0.2.0",
  "private": true,
  "description": "A CLI for AI agents to operate PTC Creo Parametric — model lifecycle, typed parameters and dimensions, feature states, material assignment, assembly trees, drawings and views, and STEP/IGES/DXF/PDF exports. Guarded writes with read-back verification and structured output, built for LLM tool use.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/fatecannotbealtered/creo-cli.git"
  },
  "bin": {
    "creo-cli": "scripts/run.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "python scripts/test.py",
    "version": "node scripts/sync-version.js",
    "check-version": "node scripts/check-version.js",
    "prepublishOnly": "python scripts/release_gate.py"
  }
}
